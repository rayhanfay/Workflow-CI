name: diabetes-classification-ci

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      n_estimators: {type: int, default: 300}
      max_depth: {type: int, default: 10}
      data_path: {type: string, default: "diabetes_preprocessing"}
    command: "python modelling.py --n_estimators {n_estimators} --max_depth {max_depth} --data_path {data_path}"
